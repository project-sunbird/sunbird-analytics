package org.ekstep.ilimi.analytics.framework.fetcher

import org.ekstep.ilimi.analytics.framework.Query;

/**
 * @author Santhosh
 */
object S3DataFetcher {
    
    def getObjectKeys(queries: Array[Query]) : List[String] = {
        null;
    }
}